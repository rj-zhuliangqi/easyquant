# 数据备份历史

> 只用于回滚参考。备份文件放在 `data/.backup-pre-*.db`，与主 db 同尺寸，
> 占用磁盘较大（每次全量拷贝一次）。

| 备份文件 | 触发动作 | 备注 |
|---|---|---|
| `.backup-pre-sprint-a.db` | Sprint A（P0-6 补强 sanitize + 行动优先级字段 + 笔记 DELETE） 开始前 | 包含 P0–P5 前 7 个分支全部上线后的 schema |
